import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Prueba B5-U1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
